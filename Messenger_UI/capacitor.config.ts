import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.messengerui.app',
  appName: 'messenger-uinpm install @capacitor/android -fnpx cap add android',
  webDir: 'dist/messenger-ui',
  bundledWebRuntime: false
};

export default config;
